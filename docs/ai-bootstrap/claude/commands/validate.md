---
description: Run type-check, lint, and tests
---

Run the project validation gate:

```bash
npm run validate
```

If any step fails, fix issues and re-run until all pass.

User context: $ARGUMENTS
